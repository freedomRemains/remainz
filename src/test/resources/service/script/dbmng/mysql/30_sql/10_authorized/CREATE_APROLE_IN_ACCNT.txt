CREATE TABLE IF NOT EXISTS APROLE_IN_ACCNT(
	APROLE_IN_ACCNT_ID INT NOT NULL AUTO_INCREMENT,
	ACCNT_ID INT,
	APROLE_ID INT,
	VERSION INT,
	IS_DELETED INT,
	CREATED_BY VARCHAR(128),
	CREATED_AT DATETIME,
	UPDATED_BY VARCHAR(128),
	UPDATED_AT TIMESTAMP,
	PRIMARY KEY(APROLE_IN_ACCNT_ID)
);
